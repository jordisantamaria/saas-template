Create a new feature module in the app.

1. Create directory `apps/web/src/features/$ARGUMENTS/`
2. Create `components/` subdirectory
3. Create `actions.ts` with server actions using `createAction()` wrapper
4. Create component files as needed
5. Wire up to pages
6. Run `pnpm typecheck` to verify

Module name: $ARGUMENTS
